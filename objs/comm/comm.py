#暂时先将各UL信道看做正交的，后续可加入物理信道对象，让所有抽象信道在物理信道上传输，此时就可考虑信道直接的干扰
class Comm():
    def __init__():
        uplinkchannels = upchannellist
        downlinkchannel = [channel(channelstateofDL)]        
    class Channel(channelstate):
        def channelin():
        def channelout():
        def infer_channel_speed():
            return channel_speed
    
   